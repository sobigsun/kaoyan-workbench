<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== 通知 & 前台服务所需权限 ===== -->
    <!-- 启动前台服务（所有版本） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Android 14+ 前台服务类型声明（番茄钟属于 general 用途，用 SPECIAL_USE） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <!-- Android 13+ 运行时请求通知显示权限（用户必须点击"允许"，否则通知栏什么都不会显示） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 番茄钟结束震动提示 -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- 极端省电模式下让倒计时精度更高（可选，没有也能跑） -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- 已装的 INTERNET -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 开机自启：把今日任务重新推送到通知栏（可选，不强制） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:name=".KaoyanApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ===== 番茄钟前台服务 ===== -->
        <service
            android:name=".PomodoroForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="pomodoro_timer:番茄钟倒计时常驻通知栏显示"/>
        </service>

        <!-- ===== 任务通知栏「完成」按钮广播接收器 ===== -->
        <receiver
            android:name=".TaskDoneReceiver"
            android:exported="false"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

</manifest>
